// jest.setup.ts

// Import các custom matchers của jest-dom (ví dụ: .toBeInTheDocument(), .toHaveClass())
// Điều này giúp bạn viết các assertions dễ đọc hơn khi kiểm tra DOM.
import '@testing-library/jest-dom';

// Bạn có thể thêm các cài đặt global khác ở đây nếu cần
// Ví dụ: mock một số API global, thiết lập giá trị mặc định, v.v.